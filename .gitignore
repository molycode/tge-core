# Build directories
build/
cmake-build-*/

# CMake generated files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps/
CMakeUserPresets.json

# Build output
*.o
*.obj
*.lo
*.slo
*.ko
*.lib
*.a
*.la
*.so
*.so.*
*.dll
*.dylib

# The logger writes relative to the working directory, so running a binary from the repo root lands here
logs/
*.log

# IDE and editor files
.vscode/
.idea/
.vs/
.qtcreator/
CMakeLists.txt.user
*.autosave
*.user
*.creator.user*
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak
*.cache
.cache/

# Claude Code — commands are shared through the repo; the personal layer is not
CLAUDE.local.md
.mcp.json
.claude/*.json

# Session scratch (project-specific, not for repo). docs/ is NOT scratch: it is the one repository every
# consumer takes, so the pool's charter and this module's own mechanism notes belong in it.
tasks/
