# Core's own proof. Nothing here may name a module: core sits below every one of them, so a test that needs
# a renderer, a scene or a window belongs to whoever composes those.

add_subdirectory(unit)
